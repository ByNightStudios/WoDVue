/*
 * MyElders Messages
 *
 * This contains all the text for the MyElders container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MyElders';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MyElders container!',
  },
});
