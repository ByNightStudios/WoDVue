// import { selectOpsContainerDomain } from '../selectors';

describe('selectOpsContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
