/*
 * OpsContainer Messages
 *
 * This contains all the text for the OpsContainer container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.OpsContainer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the OpsContainer container!',
  },
});
