/*
 * DataManagerUnAssignedElderPlanPage Messages
 *
 * This contains all the text for the DataManagerUnAssignedElderPlanPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DataManagerUnAssignedElderPlanPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Operate Elder plans!',
  },
});
