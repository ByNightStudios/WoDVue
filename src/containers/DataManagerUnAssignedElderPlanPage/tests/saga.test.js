/**
 * Test sagas
 */

/* eslint-disable redux-saga/yield-effects */
// import { take, call, put, select } from 'redux-saga/effects';
// import dataManagerUnAssignedElderPlanPageSaga from '../saga';

// const generator = dataManagerUnAssignedElderPlanPageSaga();

describe('dataManagerUnAssignedElderPlanPageSaga Saga', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
