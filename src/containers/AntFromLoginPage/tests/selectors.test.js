// import { selectAntFromLoginPageDomain } from '../selectors';

describe('selectAntFromLoginPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
