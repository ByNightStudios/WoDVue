/*
 * Elders Messages
 *
 * This contains all the text for the Elders container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Elders';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Elders container!',
  },
});
