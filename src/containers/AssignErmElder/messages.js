/*
 * AssignErmElder Messages
 *
 * This contains all the text for the AssignErmElder container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AssignErmElder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Select ERM',
  },
});
