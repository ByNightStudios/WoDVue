// import { selectAssignErmElderDomain } from '../selectors';

describe('selectAssignErmElderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
