/*
 * PlanReport Messages
 *
 * This contains all the text for the PlanReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PlanReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'PlanReport!',
  },
});
