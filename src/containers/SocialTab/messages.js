/*
 * SocialTab Messages
 *
 * This contains all the text for the SocialTab container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SocialTab';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SocialTab container!',
  },
});
