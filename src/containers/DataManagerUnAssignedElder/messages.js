/*
 * DataManagerUnAssignedElder Messages
 *
 * This contains all the text for the DataManagerUnAssignedElder container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DataManagerUnAssignedElder';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DataManagerUnAssignedElder page container!',
  },
});
