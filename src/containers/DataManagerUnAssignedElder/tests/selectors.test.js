// import { selectDataManagerUnAssignedElderDomain } from '../selectors';

describe('selectDataManagerUnAssignedElderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
