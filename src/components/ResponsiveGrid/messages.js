/*
 * ResponsiveGrid Messages
 *
 * This contains all the text for the ResponsiveGrid component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ResponsiveGrid';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ResponsiveGrid component!',
  },
});
