/*
 * EmergencyMilestones Messages
 *
 * This contains all the text for the EmergencyMilestones component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EmergencyMilestones';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmergencyMilestones component!',
  },
});
