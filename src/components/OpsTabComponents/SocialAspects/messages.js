/*
 * SocialAspects Messages
 *
 * This contains all the text for the SocialAspects component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SocialAspects';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SocialAspects component!',
  },
});
