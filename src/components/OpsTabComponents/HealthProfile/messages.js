/*
 * HealthProfile Messages
 *
 * This contains all the text for the HealthProfile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HealthProfile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HealthProfile component!',
  },
});
