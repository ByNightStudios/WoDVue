/*
 * SalesInfo Messages
 *
 * This contains all the text for the SalesInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SalesInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SalesInfo component!',
  },
});
