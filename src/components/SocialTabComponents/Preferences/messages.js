/*
 * Preferences Messages
 *
 * This contains all the text for the Preferences component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Preferences';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Preferences component!',
  },
});
