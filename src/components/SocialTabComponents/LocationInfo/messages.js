/*
 * LocationInfo Messages
 *
 * This contains all the text for the LocationInfo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LocationInfo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LocationInfo component!',
  },
});
