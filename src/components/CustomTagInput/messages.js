/*
 * CustomTagInput Messages
 *
 * This contains all the text for the CustomTagInput component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CustomTagInput';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomTagInput component!',
  },
});
